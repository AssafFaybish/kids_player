<div dir="rtl">

# 📲 התקנה בטאבלט ושחרור גרסאות — המדריך המעשי

*(הרקע המלא — למה לא חנות, כללי חתימה — נמצא ב-[PUBLISHING.md](PUBLISHING.md))*

---

## חלק א — התקנה ראשונה של גרסת release בטאבלט (חד-פעמי!)

ההתקנה הראשונה מחליפה אפליקציה ישנה שחתומה במפתח אחר, ולכן אנדרואיד מחייב
הסרה — שמוחקת את כל הנתונים. לכן הסדר קריטי:

1. **גיבוי**: באפליקציה הישנה ← 🔒 מסך הורים ← טאב "הוספה" ← **ייצוא**.
   הטקסט מועתק ללוח — הדביקו אותו בפתק/קובץ ושמרו (למשל ב-Google Keep).
2. **הסרה**: הסירו את האפליקציה הישנה (לחיצה ארוכה על האייקון ← הסרה).
3. **העברת הקובץ**: את `kids-player-v1.0.0.apk` (בתיקיית הפרויקט במחשב) העלו
   לגוגל דרייב או שלחו לעצמכם בוואטסאפ, ופתחו בטאבלט.
4. **התקנה**: הקישו על הקובץ. אנדרואיד יבקש אישור חד-פעמי
   **"התקנה מאפליקציות לא ידועות"** עבור דרייב/וואטסאפ — אשרו והתקינו.
5. **שחזור**: פתחו את האפליקציה ← מסך הורים ← **ייבוא** ← בחרו את קובץ הגיבוי.
   הכול חוזר: הרשימה, המחיקות, המתנות שכבר נפתחו.

> מכאן והלאה — עדכונים מגיעים **מתוך האפליקציה** בכפתור אחד. לא תצטרכו את
> ההליך הזה שוב.

### הגדרות מומלצות בטאבלט (חד-פעמי)
- **הצמדת אפליקציה** (הגדרות ← אבטחה ← App pinning) — הילד לא יוצא ממנה.
- **כיבוי מסך אחרי 5-10 דקות** — האפליקציה משאירה את המסך דולק בזמן ניגון
  בעצמה; ההגדרה קובעת מה קורה כשלא מנגנים.

---

## חלק ב — שחרור גרסה חדשה

### שלב 1: בניית ה-APK (במחשב)

```bash
# 1. העלאת מספר הגרסה — מקור האמת היחיד (הכול נגזר ממנו אוטומטית)
npm version patch --no-git-tag-version        # 1.0.0 → 1.0.1 (או minor/major)

# 2. טסטים — חובה ירוק
npm test

# 3. בנייה חתומה
npm run apk:release

# 4. אימות חתימה — חובה לפני כל פרסום!
npm run apk:verify                             # חייב להדפיס "Verifies"

# 5. שם הקובץ לפי המוסכמה שהאפליקציה מחפשת
cp android/app/build/outputs/apk/release/app-release.apk kids-player-v1.0.1.apk

# 6. שמירת הגרסה בגיט
git add package.json package-lock.json && git commit -m "release v1.0.1" && git push
```

### שלב 2: פרסום ב-GitHub — שתי דרכים

#### דרך א' — פקודה אחת (מטרמינל מחובר לחשבון devfassaf)

```bash
gh release create v1.0.1 kids-player-v1.0.1.apk --repo devfassaf/kids_player -t "v1.0.1" -n "מה חדש: ..."
```

#### דרך ב' — דרך האתר (2-3 דקות)

1. גלשו אל **github.com/devfassaf/kids_player/releases/new**
   (או: עמוד ה-repo ← "Releases" בעמודה הימנית ← "Draft a new release").
2. **Choose a tag** ← הקלידו `v1.0.1` (בדיוק כך, עם v קטנה) ← לחצו על
   **"Create new tag: v1.0.1 on publish"** שמופיע. Target נשאר `main`.
3. **Release title**: `v1.0.1`.
4. **Describe this release**: מה חדש, בעברית — הטקסט הזה מוצג להורה במסך
   העדכון באפליקציה.
5. גררו את `kids-player-v1.0.1.apk` לתיבת
   **"Attach binaries by dropping them here or selecting them"** והמתינו
   לסיום ההעלאה (פס התקדמות ואז שם הקובץ ברשימה).
   ⚠️ **שם הקובץ חשוב**: `kids-player-v<גרסה>.apk` — זו התבנית שהאפליקציה מחפשת.
6. ודאו ש-**"Set as the latest release"** מסומן (ברירת המחדל).
7. **Publish release** (הכפתור הירוק). לא "Save draft" — טיוטה בלתי נראית לאפליקציה!

### שלב 3: קורה מעצמו

בכל טאבלט מותקן: בתוך עד 6 שעות (או מיידית בלחיצה על "בדיקת עדכון" במסך
ההורים ← הגדרות) יופיע "יש גירסה חדשה 🎉" ← "⬇️ עדכון גירסה" ← הורדה עם
אחוזים ← מסך ההתקנה של אנדרואיד ← מאשרים ← מעודכן. הנתונים נשמרים
(אותה חתימה = עדכון במקום, בלי הסרה).

### כללי ברזל

| כלל | למה |
|---|---|
| לעולם לא לאבד את `~/.keystores/` | אובדן = אף עדכון לא יותקן לעולם ([PUBLISHING.md](PUBLISHING.md)) |
| מספר גרסה תמיד עולה | אנדרואיד מסרב לשנמוך (`INSTALL_FAILED_VERSION_DOWNGRADE`) |
| תמיד `npm run apk:verify` לפני פרסום | APK לא חתום/חתום-debug יתקע את כל המכשירים |
| בטא? `gh release create --prerelease` | האפליקציות מתעלמות מ-prerelease אוטומטית |

</div>
